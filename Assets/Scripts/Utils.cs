using System.Collections;
using UnityEngine.SceneManagement;

public class Utils
{
    public enum Objects {
        Bird = 3, // 3 stands for the bird we want actually check that is the bird object to pass through the pipe
        Pipe = 6,
    }

    public enum Scenes {
        GameScene = 0,
    }
}
